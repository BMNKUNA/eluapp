{
  "compilerOptions": {
    "target": "ES2017",                     // Specifies the target JavaScript version (ES2017).
    "lib": ["dom", "dom.iterable", "esnext"], // Defines the library files to be included in the build.
    "allowJs": true,                        // Allows JavaScript files to be compiled.
    "skipLibCheck": true,                   // Skip type checking of declaration files for performance.
    "strict": true,                         // Enables all strict type-checking options.
    "noEmit": true,                         // Do not output any files, useful for type checking only.
    "esModuleInterop": true,                // Enables compatibility with commonjs-style modules.
    "module": "esnext",                     // Use ESNext module resolution.
    "moduleResolution": "node",             // Ensures module resolution is based on Node.js.
    "resolveJsonModule": true,              // Allows importing JSON modules.
    "isolatedModules": true,                // Ensures that all files can be transpiled independently.
    "jsx": "preserve",                     // Keeps JSX syntax intact for React.
    "incremental": true,                    // Enables incremental compilation for faster builds.
    "plugins": [
      {
        "name": "next"                      // Adds the Next.js plugin for TypeScript.
      }
    ],
    "paths": {
      "@/*": ["./*"]                        // Maps the @ alias to the root of the project.
    }
  },
  "include": [
    "next-env.d.ts",                        // Includes Next.js environment types.
    "**/*.ts",                              // Includes all TypeScript files.
    "**/*.tsx",                             // Includes all TypeScript files with JSX.
    ".next/types/**/*.ts"                   // Includes types generated by Next.js.
  ],
  "exclude": [
    "node_modules"                          // Excludes node_modules from compilation.
  ]
}
